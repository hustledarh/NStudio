enum HomeProductType {
  photoCollage(label: "Photo Collage"),
  lyricFrames(label: "Lyric Frames"),
  scentedCandles(label: "Scented Candles");

  final String label;
  const HomeProductType({required this.label});
}
